import { style } from "@vanilla-extract/css";

export const wrapperStyles = style({
  display: "flex",
  flexDirection: "column",
  gap: 12,
});

export const block1Styles = style({
  display: "flex",
  flexDirection: "column",
  gap: 16,
});
