import { style } from "@vanilla-extract/css";

export const wrapperStyles = style({
  overflowY: "auto",
  position: "relative",
});

export const tableStyle = style({
  borderSpacing: 0,
  width: "100%",
  minHeight: 40,
});
