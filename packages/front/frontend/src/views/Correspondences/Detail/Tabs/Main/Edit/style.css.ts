import { style } from "@vanilla-extract/css";

export const wrapperStyles = style({
  display: "flex",
  gap: 16,
  alignItems: "flex-start",
});

export const mainInfoCardStyles = style({
  flex: 1,
});

export const additionalInfoCardStyles = style({
  width: 390,
});
