import { style } from "@vanilla-extract/css";

export const typeSelectPopupStyles = style({
  maxWidth: 300,
});

export const lastRevisionStatusStyles = style({
  maxWidth: 250,
});

export const lastRevisionStatusSelectPopupStyles = style({
  maxWidth: 250,
});
