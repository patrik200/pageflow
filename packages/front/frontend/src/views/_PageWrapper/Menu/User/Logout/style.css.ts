import { style } from "@vanilla-extract/css";

export const buttonStyles = style({
  padding: 6,
  height: "unset",
});

export const logoutButtonIconStyles = style({
  width: 20,
  height: 20,
  transform: "rotateZ(180deg)",
});
