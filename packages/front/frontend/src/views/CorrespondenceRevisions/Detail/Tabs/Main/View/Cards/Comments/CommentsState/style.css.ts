import { style } from "@vanilla-extract/css";

export const contentWrapperStyles = style({
  display: "flex",
  flexDirection: "column",
  gap: 48,
});

export const commentsWrapperStyles = style({
  display: "flex",
  flexDirection: "column",
  gap: 20,
});
