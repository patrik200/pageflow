import { globalThemeColorVars } from "@app/ui-kit"
import { style } from "@vanilla-extract/css"

export const buttonStyles = style({
    backgroundColor: globalThemeColorVars.red,
    marginRight: 12
})
