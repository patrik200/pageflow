import { style } from "@vanilla-extract/css";

export const contentWrapperStyles = style({
  display: "flex",
  flexDirection: "column",
  gap: 40,
  flex: 1,
});

export const discussionsWrapperStyles = style({
  display: "flex",
  flexDirection: "column",
  gap: 20,
});
