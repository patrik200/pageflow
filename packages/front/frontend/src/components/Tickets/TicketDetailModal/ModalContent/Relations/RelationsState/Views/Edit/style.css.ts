import { style } from "@vanilla-extract/css";

export const wrapperStyles = style({
  display: "flex",
  flexDirection: "column",
  flex: 1,
});

export const createTicketRelationButtonStyles = style({
  alignSelf: "flex-start",
});
