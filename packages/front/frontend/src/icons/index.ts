import { addInternalIcon } from "@app/ui-kit";

const folderIcon = require("./list/folder.svg").default;

addInternalIcon("folder", folderIcon);

// eslint-disable-next-line import/no-anonymous-default-export
export default null;
