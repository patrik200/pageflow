import { style } from "@vanilla-extract/css";

export const termsContentWrapperStyles = style({
  display: "flex",
  flexDirection: "column",
  gap: 40,
  maxWidth: 800,
  width: "100%",
  margin: "0 auto",
  marginBottom: 40,
});
